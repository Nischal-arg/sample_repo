name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: ActionDebugger By Warpbuild
      uses: Warpbuilds/gha-debug@v1.2.0-beta
      with:
          detached: true